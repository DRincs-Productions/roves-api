/**
 * The Roves engine shell version this version of `@drincs/roves-api` targets — bumped
 * every time a new shell version is published (see the engine repo's own CLAUDE.md,
 * "Cutting a versioned release" section). This is a static compatibility marker, not a
 * live query against the running shell — `roves:` has no "get version" command today.
 */

/** e.g. `"v0.1.0"` — matches a tag at https://github.com/DRincs-Productions/roves/releases */
export const COMPATIBLE_SHELL_VERSION = "v0.3.0";
